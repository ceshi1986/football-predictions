# 模拟投注功能说明文档

## 功能概览

fp-repo 新增竞彩足球模拟投注全链路功能，包含：
- 🧮 **手动投注计算器**（前端，5种玩法+混合过关）
- 🤖 **每日自动模拟单**（脚本，基于V6.9预测策略）
- ✅ **赛后自动结算**（对接现有赛果回填流程）
- 📊 **战绩统计展示**（前端+JSON，累计/分组/盈亏曲线）

**重要提示：模拟投注仅供策略验证，不构成投注建议。**

---

## 新增文件清单

| 文件路径 | 说明 |
|---------|------|
| `codeact/scripts/sporttery_odds_fetch.py` | 竞彩官方赔率抓取脚本 |
| `codeact/scripts/bet_sim_engine.py` | 模拟投注核心引擎（自动单+结算+统计） |
| `bet_sim.js` | 前端投注计算器+战绩JS模块 |
| `index.html` | 页面新增投注+战绩Tab（已备份 .bak.20260910_betsim） |
| `data/sporttery_odds/latest.json` | 最新赔率数据（聚合） |
| `data/sporttery_odds/YYYYMMDD.json` | 每日赔率数据（按日） |
| `data/bet_sim/stats.json` | 战绩统计（供前端读取） |
| `data/bet_sim/bets.json` | 投注单列表（供前端读取） |
| `predictions/bets/YYYYMMDD.json` | 每日自动模拟单存档 |

### 已备份文件
- `index.html.bak.20260910_betsim`

---

## 1. 赔率抓取 (sporttery_odds_fetch.py)

### 数据源
竞彩官方接口：`https://webapi.sporttery.cn/gateway/jc/football/getMatchCalculatorV1.qry`
- 参数：`poolCode=had,hhad,crs,ttg,hafu&channel=c`
- 云端可直接访问（已验证），无需走Windows住宅机兜底

### 覆盖玩法
| 代码 | 玩法 | 说明 |
|------|------|------|
| had | 胜平负 | h/d/a 三项固定奖金 |
| hhad | 让球胜平负 | 含让球数(goalLine) |
| crs | 比分 | 31个选项（含胜/平/负其他） |
| ttg | 总进球 | 0-7+ 共8档 |
| hafu | 半全场 | 9个组合 |

### 单关标识
每场每玩法在 `poolInfo` 中包含：
- `bettingSingle`: 是否开售单关
- `bettingAllup`: 是否支持串关

### 调用方式
```bash
python codeact/scripts/sporttery_odds_fetch.py display_only /path/to/fp-repo
```

### 调度建议
- 频率：每30分钟一次（与Kelly更新同步）
- 挂载：可加入 kelly_update 调度流程

---

## 2. 模拟投注核心引擎 (bet_sim_engine.py)

### 功能模式
| mode | 说明 |
|------|------|
| `auto` | 生成自动单 + 结算 + 统计导出（默认） |
| `generate` | 仅生成自动模拟单 |
| `settle` | 仅结算待结算投注单 + 统计导出 |
| `stats` | 仅重新计算统计并导出 |

### 调用方式
```bash
python codeact/scripts/bet_sim_engine.py display_only /path/to/fp-repo auto
```

### 状态库
- 路径：`/tmp/bet_sim.db`（SQLite，挂载盘不支持mmap I/O）
- 表结构：
  - `bets`: 投注单主表（bet_id主键，幂等）
  - `daily_stats`: 每日统计表
  - `meta`: 元数据表（计数器等）

### 自动模拟单生成规则
基于V6.9锁定预测数据（`data/500com_daily/YYYYMMDD/locked_predictions.json`）：

1. **单选场 + 单关开售** → 单关单选 × 5倍
2. **双选场 + 单关开售** → 单关双选 × 5倍
3. **信心最高的3场单选场** → 2串1（3个组合）× 5倍
4. **非单关单选场** → 不单独下单，纳入串关候选池
5. 赔率缺失场次自动跳过并告警

> 规则参数可在脚本顶部 `config` 中调整。

### 结算逻辑
- **数据源优先级**：backtest_master_table_dedup.json → schedule.json
- **结算公式**：奖金 = 2元 × 各场命中赔率连乘 × 倍数
- **串关规则**：全中才算中奖，一场未中整组作废
- **双选/多选**：枚举所有组合，命中组合独立计奖后求和
- **幂等保证**：status='pending'才结算，已结算的不会重复

### 奖金封顶规则
| 场次 | 单注封顶 |
|------|----------|
| 1场 | 10万元 |
| 2-3场 | 20万元 |
| 4-5场 | 50万元 |
| 6场以上 | 100万元 |

### 金额精度
- 所有金额以 **分** 为单位存储（整数），避免浮点误差
- 前端展示时除以100转为元

---

## 3. 前端投注计算器

### 位置
页面底部新增「模拟投注」板块，三个Tab：
- 🧮 投注计算器
- 🤖 自动模拟单
- 📊 战绩统计

### 玩法切换
支持5种单玩法 + 混合过关：
1. 胜平负
2. 让球胜平负
3. 比分
4. 总进球
5. 半全场
6. 🎲 混合过关（不同比赛可选不同玩法）

### 功能特性
- ✅ 场次列表显示单关角标
- ✅ 点击选项加入/移出投注单
- ✅ 实时计算注数、金额、理论最高奖金
- ✅ 单关限制校验：非单关场选单关时拦截并提示
- ✅ 串关方式：单关/2串1/3串1/4串1
- ✅ 倍数1-50可调
- ✅ 奖金封顶自动计算与提示
- ✅ 投注单localStorage自动保存
- ✅ 支持导出JSON

---

## 4. 战绩统计

### 前端展示
- **总览卡片**：累计投入、累计回收、累计盈亏、收益率、总胜率、待结算
- **近30天盈亏曲线**：Canvas绘制（纯原生，无依赖）
- **分组统计表**：按玩法+串关方式分组（注数/胜率/投入/回收/盈亏/收益率）
- **最近投注记录**：最近20条记录列表

### 数据来源
前端读取 `data/bet_sim/stats.json` 和 `data/bet_sim/bets.json`。

### 重新生成统计
```bash
python codeact/scripts/bet_sim_engine.py display_only /path/to/fp-repo stats
```

---

## 5. 调度挂载方式

### 推荐调度流程
```
每30分钟:
  1. sporttery_odds_fetch.py  → 更新赔率数据
  2. kelly_update.py          → Kelly指数更新（现有）
  3. bet_sim_engine.py auto   → 生成新模拟单 + 结算已有单 + 更新统计
```

### 赛前60分钟触发
- 自动模拟单依赖 `locked_predictions.json`
- 与现有预测锁定逻辑同频触发
- 幂等设计：已生成的不会重复生成

### 赛后结算触发
- 每次赛果回填后调用 `bet_sim_engine.py settle`
- 或在每日预测流程末尾自动调用

---

## 6. 自测验证结果

### 核心逻辑测试（46项全通过）
- ✅ 投注金额计算（单关/串关/双选/倍数）
- ✅ 最高奖金计算（单场/多场连乘）
- ✅ 奖金封顶规则（单关10万/2-3场20万/4-5场50万/6+场100万）
- ✅ 胜平负结果判定
- ✅ 让球胜平负结果判定（正让/受让）
- ✅ 比分结果判定（含胜/平/负其他）
- ✅ 总进球结果判定
- ✅ 半全场结果判定
- ✅ 结算逻辑（单关单选命中/未中、双选、2串1全中/一场未中）

### E2E链路测试
- ✅ 赔率抓取→落盘 正常
- ✅ 自动单生成→保存DB 正常
- ✅ 赛后结算→状态更新 正常
- ✅ 统计导出→JSON 正常
- ✅ 前端数据加载→展示 正常

---

## 7. 约束遵守情况

- ✅ **备份铁律**：index.html已备份 .bak.20260910_betsim
- ✅ **只做竞彩**：北单场次不纳入模拟投注
- ✅ **接口失败兜底**：赔率缺失场次禁止入模拟单并日志告警
- ✅ **结算幂等**：status字段控制，重复运行安全
- ✅ **金额精度**：全部分单位整数计算
- ✅ **不改动现有逻辑**：Kelly抓取/V6.9预测/赛果回填均未修改
- ✅ **移动端适配**：响应式布局，投注单在移动端自动堆叠
- ✅ **显著警示**：页面顶部+底部均有"模拟投注仅供策略验证"标注
- ✅ **单关校验**：前端+后端双重校验单关标识
